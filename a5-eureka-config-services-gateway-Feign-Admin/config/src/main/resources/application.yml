server:
    port: 8888

spring:
    application:
        name: config-server
    profiles:
        active: native
    cloud:
        config:
            discovery:
                enabled: true
            server:
                native:
                    search-locations: C:\marcos\micros\a5-eureka-config-services-gateway-Feign-Admin\properties\
